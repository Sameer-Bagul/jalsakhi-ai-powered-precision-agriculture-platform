/**
 * DEMO MODE FLAG
 * Set to `true`  → app runs 100% offline with dummy data (no server, no API keys needed)
 * Set to `false` → app uses real backend + OpenWeatherMap APIs
 */
export const DEMO_MODE = true;
