# Required: Groq API key for the LLM (get from https://console.groq.com)
GROQ_API_KEY=your_groq_api_key_here

# Optional: ML model API base URLs (defaults shown). Set if your services run elsewhere.
# Crop Water API - run with: uvicorn main:app --host 0.0.0.0 --port 8001 (from Crop_Water_Model)
# CROP_WATER_API_URL=http://localhost:8001

# Soil Moisture API - run with: uvicorn api:app --host 0.0.0.0 --port 8002 (from soil_moisture_model)
# SOIL_MOISTURE_API_URL=http://localhost:8002

# Village Water Allocation API - run with: uvicorn api:app --host 0.0.0.0 --port 8003 (from village_water_allocation)
# VILLAGE_WATER_API_URL=http://localhost:8003
