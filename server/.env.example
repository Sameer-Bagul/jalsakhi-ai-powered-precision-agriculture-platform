# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
# For MongoDB Atlas, use:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net

# JWT Secret Key (Generate a strong random string)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_here_change_this_in_production

# AI/ML Configuration
ML_GATEWAY_URL=http://localhost:5000
ML_API_KEY=your_internal_gateway_key

# Email Configuration (Gmail SMTP)
# 1. Enable 2FA on your Google Account
# 2. Go to App Passwords: https://myaccount.google.com/apppasswords
# 3. Generate a password for "Mail" and "Other (Custom Name)" named "JalSakhi"
SMTP_USER=sameerbagul2004@gmail.com
SMTP_PASS=agvs iilw zwgr eluj
SENDER_EMAIL=sameerbagul2004@gmail.com

# Client URL (for CORS)
CLIENT_URL=http://localhost:5173
