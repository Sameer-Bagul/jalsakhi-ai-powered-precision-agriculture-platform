# Required: key that must be sent in x-internal-key header for POST /model1, /model2, /model3
INTERNAL_API_KEY=1234567890

# Optional: server port (default 5000)
# PORT=5000

# Optional: request body size limit (default 1mb)
# BODY_LIMIT=1mb

# Optional: request timeout in milliseconds (default 60000)
# REQUEST_TIMEOUT_MS=60000

# Optional: rate limit window in ms (default 60000 = 1 minute)
# RATE_LIMIT_WINDOW_MS=60000

# Optional: max requests per window (default 100)
# RATE_LIMIT_MAX=100

# Optional: ML API URLs for unified proxy (defaults: localhost)
# Used when requesting /crop-water/*, /soil-moisture/*, /village-water/*, /chatbot/* through the gateway
# CROP_WATER_API_URL=http://localhost:8001
# SOIL_MOISTURE_API_URL=http://localhost:8000
# VILLAGE_WATER_API_URL=http://localhost:8003
# CHATBOT_API_URL=http://localhost:8004
